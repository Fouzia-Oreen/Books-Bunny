/* eslint-disable react/prop-types */
const Title = ({title}) => {
  return (
    <div>
        <h1 className='lg:text-3xl text-xl font-semibold mb-4'>{title}</h1>
    </div>
  )
}

export default Title
